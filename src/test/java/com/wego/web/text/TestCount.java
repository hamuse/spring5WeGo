package com.wego.web.text;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class TestCount {

	public static void main(String[] args) {
	/*	Scanner scn = new Scanner(System.in);
		int totalCount = scn.nextInt();
		System.out.println("totalCount 를 입력해주세요 ");
		int startRow= 0;
		int pageNum = 0;
		int endRow = 0;
		int pageSize = 5;
		int BLOCK_SIZE = 5;
		
		int pageCount = (totalCount % pageSize !=0)?(totalCount /pageSize)+1 : totalCount / pageSize;
	     startRow = (pageNum < 1)? 0 : (pageNum-1)*pageSize;
	    endRow = 0;
	    endRow =(pageNum ==pageCount)?totalCount -1:startRow+pageSize-1;
	    int blockCount = 0;  //
	    int startPage = 0;
	    int endPage = 0;
	    int blockNum = 0;
	    blockCount = (pageCount % BLOCK_SIZE !=0)?(pageCount/BLOCK_SIZE)+1 : pageCount / BLOCK_SIZE;
	    blockNum = (pageNum-1)/BLOCK_SIZE;
	    startPage = blockNum *BLOCK_SIZE +1;
	    endPage= (blockCount-1 == blockNum ) ? pageCount: startPage+(BLOCK_SIZE-1);
	    boolean existPrev = (startPage != 1 && blockNum !=0) ? true : false ;
	    boolean existNext = (blockNum < blockCount-1)? true : false;*/
	    List<Integer> pages = new ArrayList<>();
	  /*   int a = startPage;
	     int b =endPage ;
	  for(int i =startPage; i <= endPage ; i++) {
		  pages.add(i);*/
	  }
	  
	  

}
